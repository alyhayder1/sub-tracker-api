root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./bin/server ./cmd/server"
  bin = "./bin/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin", "db/migrations"]
  delay = 200

[log]
  time = true
