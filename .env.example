DATABASE_URL=postgres://dev:dev@localhost:5432/subtracker?sslmode=disable
REDIS_URL=redis://localhost:6379
PORT=8080
CLERK_SECRET_KEY=
TINK_CLIENT_ID=
TINK_CLIENT_SECRET=
# JWT_SECRET is currently unused by the server — Clerk verifies session
# tokens itself via its own JWKS, no separate shared secret needed here.
JWT_SECRET=
